# msg — 自定义消息定义

ROS 2 自定义消息接口，定义系统各层之间通信的数据结构。

## 消息类型

| 文件 | 描述 |
|------|------|
| `AgentTask.msg` | 任务指令：含建筑蓝图 JSON、确认状态标志、优先级 |
| `AgentStatus.msg` | 状态日志：执行进度、错误码、传感器数据、时间戳 |
| `BlockPose.msg` | 积木位姿：ID、位置（x,y,z）、姿态（四元数）、尺寸、校准标记 |

## 使用

消息编译后由各层节点通过 ROS 2 Topic 发布/订阅，实现层间解耦通信。
